{
  "name": "node-assignment",
  "version": "1.0.0",
  "description": "A node backend for ai-sensy assignment",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/index.js",
    "build": "cd react-client && npm install && npm run build"
  },
  "author": "Paras Jain",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/dialogflow": "^4.5.0",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "react-loader-spinner": "^4.0.0"
  },
  "engines": {
    "node": "^14.15.4"
  }
}
